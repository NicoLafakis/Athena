node_modules/
dist/
.pnpm-store/
*.log
.env
.env.*
!.env.example

# Athena brain directories (src/brain/paths.ts supports a project-local <cwd>/.athena)
.athena/
**/.athena/

# Agent harness scratch. Isolated worktrees are created at .claude/worktrees/<id>/,
# which is a full nested checkout of this repo — one `git add -A` away from being
# committed into itself.
.claude/

# Credential stores and key material — never committed, on any machine
credentials.json
credentials*.json
*.vault.json
*.pem
*.key
*.p12
*.pfx
