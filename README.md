# Athena

A customized configuration of Claude Code named Athena.

## Layout

- `CLAUDE.md` — Athena's identity and operating instructions (the config entry point)
- `settings.json` — permissions, environment, and hook wiring
- `agents/` — subagent definitions
- `skills/` — skills
- `hooks/` — hook scripts
- `commands/` — slash commands
